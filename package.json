{
  "name": "vender",
  "version": "0.0.0-locked",
  "private": true,
  "scripts": {
    "format": "stylelint --ignore-path=.gitignore --fix **/*.{css,scss} && eslint --ignore-path=.gitignore --ext=.js,.jsx,.ts,.tsx --fix . && prettier --log-level=error --write . && tsc"
  },
  "dependencies": {
    "eslint": "8.55.0",
    "prettier": "3.1.0",
    "stylelint": "15.11.0",
    "typescript": "5.3.3"
  },
  "pnpm": {
    "overrides": {
      "caniuse-lite": "1.0.30001566"
    }
  }
}
